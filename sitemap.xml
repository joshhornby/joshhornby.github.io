---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {% assign latest_post = site.posts
  | sort: "post.sitemap.lastmod"
  | last %}

  {% assign last_post_time = latest_post.path
  | last_modified_at %}

  <url>
    <loc>{{ site.url }}</loc>
    <lastmod>{{ last_post_time | date_to_xmlschema }}</lastmod>
  </url>
  <url>
    <loc>{{ site.url }}/about</loc>
  </url>
  {% for post in site.posts %}
    <url>
      <loc>{{ site.url }}{{ post.url }}</loc>
      {% if post.sitemap %}
        <lastmod>{{ post.sitemap.lastmod | remove: " " }}</lastmod>
      {% endif %}
    </url>
  {% endfor %}


  <url>
    <loc>{{ site.url }}{{ site.baseurl }}/tags</loc>
    <lastmod>{{ last_post_time | date_to_xmlschema }}</lastmod>
  </url>

  {% for tag in site.tags %}
    {% assign posts_for_tag = tag[1] %}
    {% if posts_for_tag.size > 0 %}
    {% assign sorted = posts_for_tag | sort: "last_modified_at" %}
    {% assign last_post = sorted | last %}

    {% comment %}
    {% endcomment %}
    {% assign git_time = last_post.path | last_modified_at %}

    <url>
      <loc>{{ site.url }}{{ site.baseurl }}/tags/{{ tag[0] | slugify }}</loc>
      <lastmod>{{ git_time | date_to_xmlschema }}</lastmod>
    </url>
    {% endif %}
  {% endfor %}

</urlset>
